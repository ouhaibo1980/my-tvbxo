# Cloudflare Pages 新版界面配置指南（最终版）

## 🎯 新版界面说明

Cloudflare Pages 界面已更新，配置方式如下：

| 字段 | 显示说明 | 填写内容 | 是否必需 |
|------|----------|----------|----------|
| **项目名称** | Project name | `my-tvbxo` | 必需 |
| **构建命令** | Build command (可选) | `pnpm run build` 或 `npm run build` | 可选 |
| **部署命令** | Deploy command (必需) | `npx wrangler pages deploy .next` | **必需** ⚠️ |

**⚠️ 关键变化**：
- **旧版**：部署命令留空，Cloudflare 自动部署
- **新版**：部署命令是必需字段，必须填写正确的命令
- **重要**：部署命令必须使用 `npx`，因为 wrangler 未全局安装

---

## ⚠️ 常见错误配置

### 错误 1：部署命令缺少 npx

```
❌ 部署命令: wrangler pages deploy .next
❌ 错误信息: /bin/sh: 1: wrangler: not found
```

**正确配置**：
```
✅ 部署命令: npx wrangler pages deploy .next
```

### 错误 2：部署命令使用 Workers 命令

```
❌ 部署命令: npx wrangler deploy
❌ 错误信息: It looks like you've run a Workers-specific command in a Pages project
```

**正确配置**：
```
✅ 部署命令: npx wrangler pages deploy .next
```

### 错误 2：部署命令留空（新版不允许）

```
❌ 部署命令: (留空)
❌ 错误信息: 部署命令是必需字段
```

**正确配置**：
```
✅ 部署命令: wrangler pages deploy .next
```

---

## 📋 详细配置步骤

### 步骤 1：进入项目设置

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 点击左侧菜单 **Workers & Pages**
3. 找到并点击您的项目 **my-tvbxo**

### 步骤 2：访问构建设置

1. 在项目页面，点击 **Settings** 标签
2. 在左侧边栏中，找到并点击 **Builds & deployments**
3. 点击右侧的 **Edit configurations** 按钮

### 步骤 3：配置构建设置（新版界面）

您会看到新版配置界面，请按照以下方式填写：

#### 项目名称
```
项目名称: my-tvbxo
```

#### 构建命令（可选）
```
构建命令: pnpm run build
```
- 可以使用预填充的值：`pnpm run build`
- 或改为：`npm run build`
- 两者都可以

#### 部署命令（⚠️ 必需）
```
部署命令: npx wrangler pages deploy .next
```
- ⚠️ **这是最关键的配置**
- 必须填写 `npx wrangler pages deploy .next`（注意前面的 `npx`）
- 不能使用 `npx wrangler deploy`（这是 Workers 命令）
- 不能使用 `wrangler pages deploy .next`（缺少 npx）

### 步骤 4：保存并部署

1. 点击页面底部的 **Save and Deploy** 按钮
2. 等待部署完成（2-5 分钟）

---

## 🎨 配置示例

### 正确配置示例（新版界面）

```
┌─────────────────────────────────────────────────────┐
│ 项目名称:         my-tvbxo                          │
│                                                      │
│ 构建命令 (可选):  pnpm run build                    │
│                    或: npm run build                │
│                                                      │
│ 部署命令 (必需):   npx wrangler pages deploy .next ⚠️⚠️⚠️  │
└─────────────────────────────────────────────────────┘
```

### 错误配置示例

```
┌─────────────────────────────────────────────────────┐
│ 构建命令: pnpm run build                            │
│                                                      │
│ 部署命令: wrangler pages deploy .next ❌ 错误！       │
│           缺少 npx，会导致 wrangler: not found       │
└─────────────────────────────────────────────────────┘

或

┌─────────────────────────────────────────────────────┐
│ 构建命令: pnpm run build                            │
│                                                      │
│ 部署命令: npx wrangler deploy ❌ 错误！              │
│           这是 Workers 命令，不适用于 Pages          │
└─────────────────────────────────────────────────────┘
```

---

## 🔍 验证配置是否正确

### 检查清单

部署前，请确认：

- [ ] **部署命令** 是 `npx wrangler pages deploy .next`（必须有 `npx`）
- [ ] **部署命令** 包含 `pages deploy`（不是单独的 `deploy`）
- [ ] **构建命令** 是 `pnpm run build` 或 `npm run build`
- [ ] **项目名称** 是 `my-tvbxo`

### 测试命令

在本地测试这些命令是否正确：

```bash
# 测试 1：检查构建命令
cat package.json | grep -A 5 '"scripts"'

# 应该看到：
# "scripts": {
#   "build": "next build",
#   ...
# }

# 测试 2：运行构建命令
pnpm run build

# 如果成功，输出如下：
# ✓ Compiled successfully
# ✓ Generating static pages (6/6)

# 测试 3：检查输出目录
ls -la .next
```

---

## 🚨 故障排除

### 问题 1：wrangler: not found 错误

```
/bin/sh: 1: wrangler: not found
```

**原因**：部署命令缺少 `npx`

**解决**：
1. 将部署命令改为 `npx wrangler pages deploy .next`
2. 必须包含 `npx`

### 问题 2：Workers-specific command 错误

```
✘ [ERROR] It looks like you've run a Workers-specific command in a Pages project.
```

**原因**：部署命令使用了 `npx wrangler deploy`（缺少 `pages` 子命令）

**解决**：
1. 将部署命令改为 `npx wrangler pages deploy .next`
2. 必须包含 `pages deploy`

### 问题 3：构建失败

```
Error: Build failed
```

**原因**：可能是依赖问题或代码错误

**解决**：
1. 在本地运行 `pnpm run build` 检查
2. 查看构建日志中的错误信息
3. 修复代码问题后重新提交

### 问题 4：找不到 .next 目录

```
Error: Directory .next not found
```

**原因**：构建命令执行失败或输出目录不正确

**解决**：
1. 确认构建命令执行成功
2. 检查 Next.js 配置（`next.config.ts`）
3. 确认输出目录确实是 `.next`

---

## 📖 命令对比

### Workers vs Pages 命令

| 命令类型 | Workers | Pages | 本项目使用 |
|----------|---------|-------|-----------|
| 构建命令 | `npm run build` | `npm run build` | ✅ 使用 |
| 部署命令 | `npx wrangler deploy` | `npx wrangler pages deploy .next` | ✅ 使用 Pages 命令 |

### 为什么需要 npx？

| 命令 | 说明 | 是否可用 |
|------|------|----------|
| `wrangler` | 全局安装的 wrangler CLI | ❌ 未安装 |
| `npx wrangler` | 使用 npx 运行 wrangler | ✅ 可用 |

`npx` 是 Node.js 的包执行器，会自动下载并运行指定的包，不需要预先安装。

### 为什么项目类型很重要？

- **Cloudflare Workers**：边缘计算服务，运行 JavaScript/TypeScript 在 Cloudflare 的边缘节点
- **Cloudflare Pages**：静态网站托管服务，专门用于部署前端应用（如 Next.js、React、Vue 等）

您的项目是 Next.js 应用，应该部署到 Cloudflare Pages，因此必须使用 Pages 相关的命令。

---

## ✅ 成功标志

配置正确后，部署日志应该显示：

```
Installing project dependencies: pnpm install --frozen-lockfile
Done in 11.6s
Executing user build command: pnpm run build
✓ Compiled successfully in 4.2s
✓ Generating static pages (6/6)
Success: Build command completed
Executing user deploy command: npx wrangler pages deploy .next
Uploading .next to Cloudflare Pages...
Success: Deployed!
```

部署完成后，访问：https://my-tvbxo.pages.dev

---

## 🔗 相关文档

- [QUICK-FIX.md](QUICK-FIX.md) - 快速修复指南（30 秒）
- [DEPLOYMENT.md](DEPLOYMENT.md) - 完整部署指南

---

**最后更新**：2026-01-09
**适用版本**：Next.js 16, Cloudflare Pages（新版界面）
