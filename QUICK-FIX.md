# ⚡ Cloudflare Pages 快速修复指南（最终版）

## 当前错误

```
/bin/sh: 1: wrangler: not found
```

## 🔴 问题：wrangler 命令未安装

`wrangler` 命令没有在 Cloudflare Pages 的构建环境中全局安装。

## ✅ 解决方案（30 秒内完成）

### 第 1 步：打开 Cloudflare Pages 项目设置

1. 访问：https://dash.cloudflare.com/
2. 点击左侧菜单：**Workers & Pages**
3. 点击项目：**my-tvbxo**
4. 点击顶部的：**Settings** 标签

### 第 2 步：编辑构建设置

1. 在左侧边栏中，找到并点击：**Builds & deployments**
2. 点击右侧的：**Edit configurations** 按钮

### 第 3 步：配置（⚠️ 最关键的一步）

您会看到新版的配置界面，请按照以下方式填写：

| 字段 | 显示说明 | 填写内容 | 注意事项 |
|------|----------|----------|----------|
| **项目名称** | 项目名称 | `my-tvbxo` | 保持不变 |
| **构建命令** | 可选 | `pnpm run build` | 使用预填充的值 |
| **部署命令** | 必需 | `npx wrangler pages deploy .next` | ⚠️ ⚠️ ⚠️ 最重要！ |

**⚠️ 关键说明**：
- **构建命令**：使用预填充的 `pnpm run build`
- **部署命令**：必须使用 `npx wrangler pages deploy .next`（注意前面的 `npx`）

### 第 4 步：保存并部署

1. 点击页面底部的：**Save and Deploy** 按钮
2. 等待部署完成（2-5 分钟）

---

## 🎯 正确的配置示例（最终版）

```
┌─────────────────────────────────────────────────────┐
│ 项目名称: my-tvbxo                                  │
│                                                      │
│ 构建命令 (可选):  pnpm run build                    │
│                                                      │
│ 部署命令 (必需):   npx wrangler pages deploy .next ⚠️⚠️⚠️  │
└─────────────────────────────────────────────────────┘
```

## ❌ 错误的配置示例

```
┌─────────────────────────────────────────────────────┐
│ 构建命令: pnpm run build                            │
│                                                      │
│ 部署命令: wrangler pages deploy .next ❌ 错误！       │
│           缺少 npx，会导致 wrangler: not found       │
└─────────────────────────────────────────────────────┘

或

┌─────────────────────────────────────────────────────┐
│ 构建命令: pnpm run build                            │
│                                                      │
│ 部署命令: npx wrangler deploy ❌ 错误！              │
│           这是 Workers 命令，不适用于 Pages          │
└─────────────────────────────────────────────────────┘
```

---

## 📋 检查清单

修改前，请确认：

- [ ] **部署命令** 是 `npx wrangler pages deploy .next`（必须有 `npx`）
- [ ] **部署命令** 包含 `pages deploy`（不是单独的 `deploy`）
- [ ] **构建命令** 是 `pnpm run build`
- [ ] 项目名称是 `my-tvbxo`

---

## 🔍 为什么需要 npx？

### `wrangler` vs `npx wrangler`

| 命令 | 说明 | 是否可用 |
|------|------|----------|
| `wrangler` | 全局安装的 wrangler CLI | ❌ 未安装 |
| `npx wrangler` | 使用 npx 运行 wrangler | ✅ 可用 |

`npx` 是 Node.js 的包执行器，会自动下载并运行指定的包，不需要预先安装。

---

## ❓ 常见问题

### Q: 为什么之前的错误说不要用 npx wrangler deploy？

**A**: 之前的错误是 `npx wrangler deploy`（缺少 `pages`），这是 Workers 专用命令。正确的命令是 `npx wrangler pages deploy .next`（包含 `pages` 子命令）。

### Q: `.next` 是指什么？

**A**: `.next` 是 Next.js 的构建输出目录。执行 `pnpm run build` 后，Next.js 会将编译后的文件输出到这个目录。

### Q: 可以留空部署命令吗？

**A**: 如果界面允许，可以尝试留空。但如果界面要求必须填写，请使用 `npx wrangler pages deploy .next`。

---

## 📖 需要更多帮助？

如果按照以上步骤操作后仍然失败：

1. 查看完整的构建日志
2. 检查构建输出目录是否为 `.next`
3. 参考详细文档

---

## ✅ 成功标志

配置正确后，部署日志应该显示：

```
Installing project dependencies: pnpm install --frozen-lockfile
Done in 11.6s
Executing user build command: pnpm run build
✓ Compiled successfully in 4.2s
✓ Generating static pages (6/6)
Success: Build command completed
Executing user deploy command: npx wrangler pages deploy .next
Uploading .next to Cloudflare Pages...
Success: Deployed!
```

部署完成后，访问：https://my-tvbxo.pages.dev

---

**最后更新**：2026-01-09
**适用版本**：Next.js 16, Cloudflare Pages（新版界面）
