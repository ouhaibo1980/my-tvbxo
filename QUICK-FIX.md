# ⚡ Cloudflare Pages 快速修复指南（新版界面）

## 当前错误

```
✘ [ERROR] It looks like you've run a Workers-specific command in a Pages project.
For Pages, please run `wrangler pages deploy` instead.
```

## 🔄 新版界面说明

Cloudflare Pages 界面已更新，现在显示：
- ✅ **构建命令**：可选，预填充 `pnpm run build`
- ✅ **部署命令**：必需（新版变化）

## 🔴 问题：部署命令填写错误

您的"部署命令"字段填写了 `npx wrangler deploy`，这是用于部署 Workers 的，不适用于 Pages。

## ✅ 解决方案（30 秒内完成）

### 第 1 步：打开 Cloudflare Pages 项目设置

1. 访问：https://dash.cloudflare.com/
2. 点击左侧菜单：**Workers & Pages**
3. 点击项目：**my-tvbxo**
4. 点击顶部的：**Settings** 标签

### 第 2 步：编辑构建设置

1. 在左侧边栏中，找到并点击：**Builds & deployments**
2. 点击右侧的：**Edit configurations** 按钮

### 第 3 步：配置（⚠️ 最关键的一步）

您会看到新版的配置界面，请按照以下方式填写：

| 字段 | 显示说明 | 填写内容 | 注意事项 |
|------|----------|----------|----------|
| **项目名称** | 项目名称 | `my-tvbxo` | 保持不变 |
| **构建命令** | 可选 | `pnpm run build` | 使用预填充的值，或改为 `npm run build` |
| **部署命令** | **必需** ⚠️ | `wrangler pages deploy .next` | ⚠️ ⚠️ ⚠️ 最重要！ |

**⚠️ 关键说明**：
- **构建命令**：可以使用预填充的 `pnpm run build`，也可以改为 `npm run build`
- **部署命令**：新版界面要求必须填写，应该填写 `wrangler pages deploy .next`

### 第 4 步：保存并部署

1. 点击页面底部的：**Save and Deploy** 按钮
2. 等待部署完成（2-5 分钟）

---

## 🎯 正确的配置示例（新版界面）

```
┌─────────────────────────────────────────────────────┐
│ 项目名称: my-tvbxo                                  │
│                                                      │
│ 构建命令 (可选):  pnpm run build                    │
│                     或: npm run build                │
│                                                      │
│ 部署命令 (必需):   wrangler pages deploy .next ⚠️⚠️⚠️  │
└─────────────────────────────────────────────────────┘
```

## ❌ 错误的配置示例

```
┌─────────────────────────────────────────────────────┐
│ 构建命令: pnpm run build                            │
│                                                      │
│ 部署命令: npx wrangler deploy ❌ 错误！              │
│           这会导致 Workers 专用命令错误               │
└─────────────────────────────────────────────────────┘
```

---

## 📋 检查清单

修改前，请确认：

- [ ] **部署命令** 是 `wrangler pages deploy .next`（不是 `npx wrangler deploy`）
- [ ] **构建命令** 是 `pnpm run build` 或 `npm run build`
- [ ] 项目名称是 `my-tvbxo`

---

## 🔍 关于部署命令的说明

### 为什么需要部署命令？

新版 Cloudflare Pages 要求明确指定如何部署构建产物。对于 Next.js 项目：

- **构建命令**：将 Next.js 项目编译到 `.next` 目录
- **部署命令**：将 `.next` 目录部署到 Cloudflare Pages

### 常见的部署命令选项

| 命令 | 用途 | 是否适用于本项目 |
|------|------|----------------|
| `wrangler pages deploy .next` | 部署 Pages 项目 | ✅ 推荐 |
| `npx wrangler deploy` | 部署 Workers 项目 | ❌ 不适用 |
| `npm run deploy` | 自定义部署脚本 | ❌ 未配置 |

---

## ❓ 常见问题

### Q: 部署命令显示"必需"，但我之前不需要填写？

**A**: 新版 Cloudflare Pages 界面要求必须指定部署命令。对于 Next.js 项目，应该使用 `wrangler pages deploy .next`。

### Q: 构建命令应该用 `pnpm run build` 还是 `npm run build`？

**A**: 两者都可以：
- `pnpm run build`：使用 pnpm（如果项目使用 pnpm）
- `npm run build`：使用 npm

建议使用预填充的 `pnpm run build`。

### Q: 为什么不能用 `npx wrangler deploy`？

**A**: `npx wrangler deploy` 是用于部署 Cloudflare Workers 的命令。您的项目是 Next.js 应用，应该部署到 Cloudflare Pages，所以需要使用 `wrangler pages deploy`。

### Q: `.next` 是指什么？

**A**: `.next` 是 Next.js 的构建输出目录。执行 `pnpm run build` 后，Next.js 会将编译后的文件输出到这个目录。

---

## 📖 需要更多帮助？

如果按照以上步骤操作后仍然失败：

1. 查看完整的构建日志
2. 检查构建输出目录是否为 `.next`
3. 参考详细文档

---

## ✅ 成功标志

配置正确后，部署日志应该显示：

```
Installing project dependencies: pnpm install --frozen-lockfile
Done in 11.6s
Executing user build command: pnpm run build
✓ Compiled successfully in 4.2s
✓ Generating static pages (6/6)
Success: Build command completed
Executing user deploy command: wrangler pages deploy .next
Uploading .next to Cloudflare Pages...
Success: Deployed!
```

部署完成后，访问：https://my-tvbxo.pages.dev

---

**最后更新**：2026-01-09
**适用版本**：Next.js 16, Cloudflare Pages（新版界面）
