# ⚡ Cloudflare Pages 快速修复指南

## 当前错误

```
✘ [ERROR] It looks like you've run a Workers-specific command in a Pages project.
For Pages, please run `wrangler pages deploy` instead.
```

## 🔴 问题：Deploy command 字段没有清空

您的 Cloudflare Pages 配置中仍然在 "Deploy command" 字段填写了内容，这导致了错误。

## ✅ 解决方案（30 秒内完成）

### 第 1 步：打开 Cloudflare Pages 项目设置

1. 访问：https://dash.cloudflare.com/
2. 点击左侧菜单：**Workers & Pages**
3. 点击项目：**my-tvbxo**
4. 点击顶部的：**Settings** 标签

### 第 2 步：编辑构建设置

1. 在左侧边栏中，找到并点击：**Builds & deployments**
2. 点击右侧的：**Edit configurations** 按钮

### 第 3 步：配置（⚠️ 最关键的一步）

您会看到一个表单，请按照以下方式填写：

| 字段 | 填写内容 | 注意事项 |
|------|----------|----------|
| **Build command** | `npm run build` | 完整输入，包括 npm run |
| **Build output directory** | `.next` 或留空 | 不要添加前导 / |
| **Root directory** | 留空 | 不要填写任何内容 |
| **Deploy command** | **删除所有内容，留空** | ⚠️ ⚠️ ⚠️ 最重要！ |

**注意**：Cloudflare Pages 会自动检测 Next.js 框架，不需要手动选择 Framework preset。

### 第 4 步：保存并部署

1. 点击页面底部的：**Save** 按钮
2. 等待保存成功提示
3. 触发新部署（推送代码或在 Cloudflare 中手动触发）

---

## 🎯 正确的配置示例

```
┌─────────────────────────────────────────────────────┐
│ Build command:     npm run build                   │
│ Build output dir:  .next                           │
│ Root directory:    (空，不要填写)                   │
│ Deploy command:    (空，必须清空所有内容) ⚠️⚠️⚠️     │
└─────────────────────────────────────────────────────┘
```

## ❌ 错误的配置示例

```
┌─────────────────────────────────────────────────────┐
│ Build command:     npm run build                  │
│ Build output dir:  .next                           │
│ Root directory:    (空)                             │
│ Deploy command:    npx wrangler deploy ❌ 错误！     │
└─────────────────────────────────────────────────────┘
```

---

## 📋 检查清单

修改前，请确认：

- [ ] **Deploy command 字段已完全清空**（没有任何字符）
- [ ] Build command 是 `npm run build`（不是 `Next.js` 或 `build`）
- [ ] Build output directory 是 `.next` 或留空
- [ ] Root directory 留空

---

## 🔍 如何确认 Deploy command 已清空？

在 Cloudflare Dashboard 的配置页面中：

1. 找到 "Deploy command" 字段
2. 点击该字段
3. 按 `Ctrl + A`（Windows/Linux）或 `Cmd + A`（Mac）全选
4. 按 `Delete` 或 `Backspace` 删除所有内容
5. 确认字段完全为空
6. 点击其他字段，确认没有自动填充任何内容

---

## ❓ 常见问题

### Q: 我已经清空了 Deploy command，为什么还是失败？

**A**: 可能的原因：
1. 没有保存更改（记得点击 Save 按钮）
2. 修改的是错误的配置（确保在 Settings → Builds & deployments 中）
3. 需要触发新的部署（推送新代码或手动触发）

### Q: Deploy command 留空后，代码如何部署？

**A**: Cloudflare Pages 会自动处理：
1. 执行 Build command（`npm run build`）
2. 读取 Build output directory（`.next`）
3. 自动将输出目录部署到全球 CDN

### Q: 没有看到 Framework preset 选项？

**A**: 正常！Cloudflare Pages 会自动检测 Next.js 框架，不需要手动选择。

### Q: 如果我确实需要自定义部署命令怎么办？

**A**: 对于 Next.js 项目，**不需要**自定义部署命令。Cloudflare Pages 会自动处理。

---

## 📖 需要更多帮助？

如果按照以上步骤操作后仍然失败：

1. 检查完整的构建日志
2. 参考详细文档：[CLOUDFLARE-PAGES-CONFIG.md](CLOUDFLARE-PAGES-CONFIG.md)
3. 查看故障排除：[DEPLOYMENT.md](DEPLOYMENT.md)

---

## ✅ 成功标志

配置正确后，部署日志应该显示：

```
Installing project dependencies: pnpm install --frozen-lockfile
Done in 9.9s
Executing user build command: pnpm run build
✓ Compiled successfully in 4.2s
✓ Generating static pages (6/6)
Success: Build command completed
Uploading to Cloudflare Pages...
Success: Deployed!
```

部署完成后，访问：https://my-tvbxo.pages.dev

---

**最后更新**：2026-01-09
**适用版本**：Next.js 16, Cloudflare Pages
